# Netlify redirects file
# Serve the web app at /app/* by proxying to a subdomain or different service
# Option A: If web app is hosted on a subdomain
/app/* https://app.ninjaterm.mbedded.ninja/:splat 200

# Option B: If web app is hosted on a different port/service (for development)
# /app/* http://localhost:3000/:splat 200

# Fallback: serve Docusaurus for everything else
/* /index.html 200